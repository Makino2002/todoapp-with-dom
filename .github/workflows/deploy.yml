name: CI/CD on My Windows Host

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cài đặt backend
        run: |
          cd backend
          npm install

      - name: Khởi động lại server Node.js
        shell: powershell
        run: |
          $process = Get-Process node -ErrorAction SilentlyContinue
          if ($process) {
            Stop-Process -Name node -Force
            Write-Host "⛔ Đã dừng tiến trình node.exe"
          } else {
            Write-Host "✅ Không có tiến trình node đang chạy"
          }
          Start-Process "C:\\Program Files\\nodejs\\node.exe" -ArgumentList "F:\\todoapp-with-dom\\backend\\server.js"
